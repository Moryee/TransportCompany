import { insertData, updateData, deleteData, getData, generateId  } from "./service";
import { restrict, authData, login, logout, register } from "./AuthService";

export {
    insertData, 
    updateData,
    deleteData,
    getData,
    generateId,

    restrict,
    authData,
    login,
    logout,
    register,
}